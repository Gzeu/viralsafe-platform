# Monitoring disabled intentionally
# This workflow is kept as a stub to preserve history and easy re-enable.
# To re-enable, restore previous steps and schedule.

name: ViralSafe Uptime Monitor (Disabled)

on:
  workflow_dispatch: # Allow manual run to confirm it's disabled

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Monitoring disabled
        run: echo "Uptime monitoring is disabled for this repository."